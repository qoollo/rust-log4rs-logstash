refresh_rate: 30 seconds
appenders:
  stdout:
    kind: console
    encoder:
      pattern: "{d(%Y-%m-%d %H:%M:%S):<20} {M:>20.30}:{L:>3} {h({l})}    {m}\n"
  logstash:
    kind: logstash
    hostname: localhost
    port: 5055
    buffer_size: 100
    buffer_lifetime: 1s
root:
  level: debug
  appenders:
    - stdout
    - logstash
